Features

Type-safe student data using a TypeScript interface.

Dynamically generates an HTML table using the DOM API.

Uses Webpack to bundle and compile the TypeScript code.

No type errors on build.

📁 Project Structure
.
├── dist/               # Compiled output (after Webpack build)
├── src/
│   └── main.ts         # Main TypeScript file with logic
├── index.html          # Main HTML file
├── tsconfig.json       # TypeScript configuration
├── webpack.config.js   # Webpack configuration
└── README.md           # You're here!
